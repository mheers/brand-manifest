# Logo assets

Replace the example SVGs with the approved logo exports. Keep the filenames referenced by `../../brand.json` stable unless the manifest and all consumers are updated together.

Record usage restrictions in the manifest, not by relying on folder names. Include the source files and production exports when a logo has a source format and platform-specific variants.
